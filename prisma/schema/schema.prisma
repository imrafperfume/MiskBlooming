datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "linux-musl", "linux-musl-openssl-3.0.x"]
}

// Include all schema files
// This file serves as the main schema file that includes all other schema files

// Include all schema modules
// User schema
// Product schema  
// Order schema
// Invoice schema
// Coupon schema
