model SystemSetting {
    id          String   @id @default(cuid())
    theme       String   @default("light")
    layoutStyle String   @default("full") // boxed, full
    updatedAt   DateTime @updatedAt
}
